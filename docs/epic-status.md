# Epic implementation status

> Tracks delivery of the [epic catalogue](product/05-epics-and-stories.md) against the [agile delivery plan](product/15-agile-delivery-plan.md). Product definition and architecture are baselined; implementation is a walking vertical slice with a runnable web preview and Electron shell. No epic is complete until its acceptance contract passes.

**Legend:** ✅ Completed (implemented & verified against acceptance criteria) · 🟡 Partial · ⛔ Not started

_Last updated: 2026-07-27_

| Epic | Capability | Status | Train | Notes |
|---|---|:--:|:--:|---|
| E01 | Tenancy, identity and access | 🟡 | R2 (first-party closed) | Live SAML assertion and OIDC code/PKCE federation, browser-bound replay protection, tenant-safe JIT linking, SCIM User/Group lifecycle and discovery, scoped tokens, soft deprovision/session revocation, certification evidence UI/API and Docker-free runtime verification are complete. A named Entra/Okta production pass remains an external evidence gate |
| E02 | RBAC, record sharing and segregation of duties | 🟡 | R1 (P0 closed) | Role hierarchy, profiles, permission sets/groups, object/FLS policy tables, sharing evidence, masking, SoD, maker-checker, delegated admin, access reviews, export audit and access explainers are implemented; account, contact, lead and opportunity APIs now enforce the shared record authorization boundary |
| E03 | Organization, reference and master data | 🟡 | R1 (P0 closed) | Multi-currency/rates, fiscal calendar, business hours/holidays, governed/effective-dated picklists, territory preview/activation, quotas and maker-checker governed master changes are implemented |
| E04 | Accounts, contacts, hierarchy and buying groups | 🟡 | R1 (P0 closed) | Governed account/contact CRUD and lifecycle, optimistic concurrency, soft delete, hierarchy and permission-safe rollups now sit beside buying groups, duplicate/merge evidence, consent, health and Account 360 |
| E05 | Lead capture, qualification and routing | 🟡 | R1 (P0 closed) | Single/bulk capture, detail/update/reassign/reactivate, duplicate handling, scoring, routing, SLA, conversion and disqualification enforce record authorization and atomic audit/outbox evidence |
| E06 | Opportunity and pipeline management | 🟡 | R1 (P0 closed) | Governed opportunity CRUD plus stage, close/reopen, line, split, competitor, contact-role, qualification and approval lifecycles enforce record authorization and atomic audit/outbox evidence |
| E07 | Activity, email and calendar engagement | 🟡 | R2 | First-party tasks, events, calls, notes, manual email logs and unified activity timeline are implemented. Microsoft/Google/telephony connector capture remains intentionally pending |
| E08 | Products, price books, quotes and CPQ | 🟡 | R2 | CPQ database foundation, tenant seed data, read-side product/price-book/quote APIs, server-side search/filtering/100-row pagination, quote summary metrics and React commerce workspaces are implemented. Quote authoring, generated document lifecycle and external e-sign vendor integrations remain intentionally pending |
| E09 | Contracts, orders, subscriptions and renewals | 🟡 | R3 | Contract/order/subscription schemas, tenant RLS, seed data and the `/contracts` read workspace are implemented with renewal-risk summary metrics |
| E10 | Forecasting and revenue intelligence | 🟡 | R2 | Forecast periods, submissions, snapshots, tenant seed data and the `/forecast` read workspace are implemented with submitted/weighted/risk metrics |
| E11 | Campaigns, segments and marketing alignment | 🟡 | R3 | Campaigns, segments and members now include immutable, explainable performance snapshots and operator ROI capture; visual segment authoring remains open |
| E12 | Cases, entitlements and SLA management | 🟡 | R3 | Entitlements and milestones now include idempotent overdue-milestone detection, missed-state evidence and case escalation from the service workspace |
| E13 | Partner, channel and territory management | 🟡 | R4 | Active partners can register open opportunities through a retry-safe command with partner-overlap detection and explicit protection/hold outcomes; external partner portal remains pending |
| E14 | Workflow automation, approvals and rules engine | 🟡 | R1 (partial) | The product workspace is converged onto the canonical versioned rules engine with read-only simulation and provenance-preserving restore. External webhook execution remains pending |
| E15 | Reporting, dashboards and analytics | 🟡 | R2 | Analytics Studio provides governed report building, dashboards, Jasper output and collaboration. P0E15 projection recovery, independent KPI reconciliation, grid/PDF dataset fingerprints and an evidence-based million-row production certification gate are complete. External mail transport remains a pending vendor adapter |
| E16 | AI copilot and agentic assistance | 🟡 | R2 (P0 slice) | Provider-independent prompt, recommendation and citation schemas, tenant seed data and the `/copilot` read workspace are implemented. Vendor model execution remains intentionally pending |
| E17 | Integration platform, APIs, webhooks and events | 🟡 | R2 | Outbox/Kafka relay, OpenAPI document, metrics, service credentials, SCIM service-token foundations, endpoint-contract/job/webhook-stub schemas, seed data and the `/integrations` workspace are implemented. External webhooks/connectors remain pending |
| E18 | Data migration and onboarding | 🟡 | R2 (first-party closed) | The vendor-neutral migration engine and dedicated `/migration` operator workspace cover read-only discovery, explicit/versioned mapping, zero-write dry run, relationship/history import, reconciliation reruns, exact-ledger rollback, transactional delta checkpoints, linked retry/cancel recovery, 100-row issue search/filter and onboarding. Salesforce/Zoho/HubSpot authenticated adapters remain `PENDING_VENDOR` |
| E19 | Administration, configuration, sandbox and release | 🟡 | R2 (release control plane closed) | Sandbox isolation, immutable release packages, full-diff validation, independent production approval, atomic deployment, conflict-safe rollback and restored-environment DR certification are implemented and verified; see the [E19 closure evidence](architecture/e19-sandbox-release-dr-closure.md) |
| E20 | Audit, compliance, observability and governance | 🟡 | R1 (first-party closed) | Tamper-evident audit, read/export/authentication evidence, field history, retention/legal hold, DSR, consent, Prometheus metrics and SLO rules are implemented. Docker-free DB tests prove audit immutability and tenant/outbox isolation; only an approved external Alertmanager receiver remains pending |
| E21 | Mobile and offline field access | 🟡 | R3 (first-party closed) | Permission-filtered offline packages, cache-age/checksum evidence, optimistic synchronization, explicit field-level conflicts, operator resolution, sync-run history, authorization recheck and responsive mobile UX are complete. Store signing/distribution remains external |
| E22 | BFSI vertical pack | 🟡 | R4 (requested workflow closed) | KYC requirement/expiry ownership, screening and disposition, auditable weighted risk, relationship activation gates, holdings/whitespace, expiring suitability, maker-checker overrides and exception decisions are implemented in `/packs/bfsi`. External screening feeds remain pending |
| E23 | Commodity trading vertical pack | 🟡 | R4 (origination closed) | Source-mastered counterparty/agreement/credit evidence, term/spot/tender/cargo origination, non-binding price expressions, term approval, fail-closed offer gates, tender lapse, idempotent execution queue and trade-reference acknowledgement are implemented. Named CTRM execution remains pending per [system design §11](architecture/system-design.md#11-integration-architecture-and-the-ctrm-connector) |

**0 / 23 epics completed · 23 partial (walking vertical slice) · 0 not started.**

## 2026-07-25 implementation increment

The current runnable slice now includes these additional partial deliveries:

- **2026-07-27 E21-E23 first-party workflow closure:** `V349` adds permission-filtered offline snapshots and explicit sync conflicts, the governed BFSI onboarding-to-suitability workflow, and commodity origination through idempotent execution handoff. Every command is tenant scoped, read-only roles are blocked from mutation, state changes produce audit/outbox evidence, grids use the 100-row server contract, and the dedicated responsive operator panels expose next-step gates in plain language. External mobile-store, screening-data and named CTRM delivery evidence remains `PENDING_VENDOR`; see [the closure record](architecture/e21-e23-vertical-workflow-closure.md).

- **2026-07-27 E19 release-control-plane closure:** every commercial tenant can create an isolated configuration sandbox with outbound traffic disabled, author and validate immutable release packages without target writes, use transitive maker-checker approval for PROD, deploy atomically, restore an exact baseline through conflict-safe rollback and certify a restored database against RTO/RPO, row parity, schema, checksum and outbox controls. Forced tenant RLS, governed auditor reads, administrator-only writes, audit/outbox evidence, targeted tests, a production frontend build and a live Docker-free browser/API exercise pass. Full proof and operator steps are in [the E19 closure record](architecture/e19-sandbox-release-dr-closure.md).

- **2026-07-27 P0 closure — E01-E06/E20:** core account, contact, lead and opportunity CRUD/lifecycle commands now share record-level authorization, optimistic concurrency, soft-delete and atomic audit/outbox controls. Hierarchies and rollups are permission filtered. A Docker-free PostgreSQL suite proves tenant/outbox RLS and immutable audit privileges/triggers; 635 backend tests, the production frontend build and all four WCAG/runtime browser gates pass. Named external IdP and Alertmanager certification remain `PENDING_VENDOR`.

- **E01/E02:** platform `SUPER_ADMIN` and `SUPER_AUDIT` identities, tenant switching, tenant-scoped JWTs, and read-only write blocking for auditor roles.
- **E03/E20:** account/lead master toolbar, audit drawer, immutable audit events, templates, atomic CSV bulk upload validation, governed Excel/Word/PDF exports, soft delete only, and in-use delete protection.
- **E04/E05:** account and lead lists use server-side search/filtering and 100-row pagination; grouped display remains available for the current page.
- **E17:** API CORS is locked to explicit configured origins and exposes only the headers needed by the frontend. Vendor and third-party integrations remain intentionally pending.
- **Database architecture:** physical module schemas now separate `platform`, `identity`, `crm`, `sales`, `engagement`, `governance`, `integration`, and `reference` tables. Tenant-consistent composite foreign keys and module catalog tables are in place.
- **E03 reference data:** governed value-set tables, seeded lead/status governance values, reference-data API, and Reference Data UI workspace are implemented for the preview.
- **E02/E15/E19:** RBAC screen policies, user-management cockpit, trial/company/billing administration, Jasper report downloads, email/report alert configuration and a standalone reporting project boundary are implemented. External delivery providers remain pending.
- **E07:** first-party activity timeline is implemented with tenant-scoped tasks, events, calls, notes and manual email logs; activity creation/completion writes audit/outbox events and reminder notifications.
- **E01/E02/E03/E04/E05/E06/E17/E20:** scanned and integrated the larger security, identity, org-data, account, lead, pipeline, audit/compliance and observability implementation set; compilation blockers, consent-DSR service wiring, migration constraints and runtime actuator exposure were hardened.
- **E08:** CPQ product/price-book/quote schema foundation and governed CPQ reference values are implemented through `V90`; `V91` seeds product catalogue, active price books, quote transactions, approvals, templates and guided selling prompts. `/api/v1/cpq/products`, `/api/v1/cpq/price-books`, `/api/v1/cpq/quotes` and `/api/v1/cpq/quotes/summary` now back the `/products`, `/price-books` and `/quotes` workspaces with 100-row server pagination. Quote authoring and quote/e-sign vendor integrations remain intentionally pending.
- **E09/E10/E11/E12/E18:** five additional operational workspaces are implemented through `V93` with module schemas, tenant RLS, composite foreign keys, seed data, Spring read APIs, React navigation/routes and 100-row server pagination. `/contracts`, `/forecast`, `/campaigns`, `/cases` and `/migration` now run against live Docker data. Vendor/third-party connectors and write-heavy workflows remain intentionally pending per the current integration boundary.
- **E13/E14/E15/E16/E21:** a second five-epic workspace wave is implemented through `V94` with partner/channel, automation, analytics dashboard, AI copilot and mobile/offline schemas. `/partners`, `/automation`, `/analytics`, `/copilot` and `/mobile` share the same tenant/RLS-governed workspace API and 100-row pagination contract. External partner portals, webhook execution, model-provider calls and native app-store builds remain intentionally pending.
- **E17/E19/E20/E22/E23:** the final five-surface wave is implemented through `V95` with integration contracts/jobs/webhook stubs, sandbox/release/deployment evidence, audit evidence packs/control reviews/observability signals, BFSI onboarding/holdings/screening and commodity counterparty/enquiry/term-sheet schemas. `/integrations`, `/sandbox`, `/audit`, `/packs/bfsi` and `/packs/commodity` are now live preview workspaces. Browser API reachability is hardened by proxying Docker web traffic through same-origin `/api/v1`.
- **E09/E10/E11/E12/E15:** operational workspaces now have governed Excel, Word and PDF exports from the same server-side search/status/page contract used by the UI. Every workspace export writes `WORKSPACE_EXPORT` audit evidence with module, format, filter criteria and row count.
- **E19/E21:** the Electron desktop shell now has a reproducible local publish pipeline that packages the current React production build into a portable Windows desktop folder and zip. Signing/store release remains pending because it requires external certificates and vendor accounts.
- **Cross-workspace UX:** all epic workspace grids now include status grouping plus the existing full-size/restore data view control, so large-page reviews can expand without breaking the surrounding shell.
- **E04/E05/E08/E15/E20:** Account 360 is now exposed through tenant-scoped detail and hierarchy APIs with a right-side drawer in `/accounts`; leads can be disqualified only with a governed reason and optional future recycle date; quotes can be downloaded as PDF, Word or Excel directly from `/quotes`; both lead disqualification and quote document download write immutable audit evidence/outbox events where applicable.
- **E10/E12/E14/E18/E21:** the operational workspace command layer now adds governed Forecast Submit, Case Resolve, Automation Simulate, Migration Validate and Mobile Sync Acknowledge actions. Each command is tenant-scoped, blocks read-only roles, validates status transitions, writes immutable audit evidence and emits a transactional outbox event.
- **E09/E11/E13/E16/E22:** the same governed command layer now covers Contract Activate, Campaign Complete, Partner Activate, Copilot Recommendation Accept and BFSI Onboarding Clear actions. Each action is first-party only, validates transition preconditions, captures business evidence, writes audit history and emits outbox events without relying on vendor integrations.
- **E15/E17/E19/E20/E23:** the third governed command wave now covers Dashboard Refresh, Integration Contract Verify, Sandbox Refresh, Audit Evidence Pack Export and Commodity Offer. Each command enforces module-specific gates, writes immutable audit evidence, emits outbox events and leaves external vendor/CTRM execution intentionally pending.
- **E02/E03/E04/E05/E07/E08/E15/E19/E20:** governed grid utilities are now part of the Data Grid frame itself across Accounts, Leads, Activities, CPQ, Reports, Reference Data, Administration and all epic workspaces. Group, Audit, Export Excel, Export Word, Export PDF and Full size/Restore stay in the sticky grid header in both normal and expanded views.
- **Cross-screen UX:** action buttons now use a universal geometry contract across the User Manual, Data Grids, Reports, Administration, notifications, menus and row actions so button width/height and touch targets remain consistent across screens and breakpoints.
- **2026-07-26 grid governance increment — E03/E04/E05/E15/E19:** master, report, reference, administration and operational workspace grids now keep column search, spread-out multi-column grouping and Excel/Word/PDF exports aligned to the same visible rows. The toolbar can still fall back to server-governed exports where no current-view rows are supplied, but pages with client-side column filters now export the filtered view the user actually sees.
- **2026-07-26 route performance increment — E01/E04/E08/E17/E19:** public access, core CRM, CPQ, integration and administration/security surfaces are now route-split into lazy chunks with an honest workspace loader. Sidebar hover/focus and keyboard navigation prefetch the intended module without reverting to an oversized first-load bundle, removing the previous Vite 500 KB chunk warning while keeping every route reachable.
- **2026-07-26 grid preference persistence increment — E02/E03/E04/E05/E08:** RBAC/security data tables, reference data, accounts, leads, activities, CPQ, reports, administration and operational workspaces now persist column filters and multi-column grouping per grid/table/module in browser storage. The persistence layer validates stored JSON, compacts blank filters and fails open when storage is unavailable, so a corrupt or locked-down browser profile cannot break the grid.
- **2026-07-26 operational resilience increment - E01/E02/E04/E15/E19:** saved grid preferences now have a supported recovery path from the avatar menu, clearing all browser-local Data Grid grouping and column-search state without manual storage editing. The global UI fault boundary now separates module-load failures from screen crashes and offers Retry, Reset views and Reload app actions, so route-split workspaces and support-heavy administration/reporting screens fail recoverably.
- **2026-07-26 governed export context increment - E03/E04/E05/E15/E19:** current-view Excel, Word and PDF downloads generated from master, CRM, CPQ, reporting, reference and administration grids now embed grid name, generated timestamp, row count, active column filters and active grouping in the exported file. The toolbar also shows the live export scope before download, closing the support gap where a file could not prove which filtered view created it.
- **2026-07-26 filter clarity increment - E02/E03/E04/E05/E19:** shared grid filters and RBAC/security data tables now render active column filters as removable chips. Users can see exactly which column is narrowing the view and remove one filter at a time without clearing grouping, sorting or other filters.
- **2026-07-26 view handoff increment - E03/E04/E05/E15/E19:** shared grid and master-data toolbars now include **Copy view**, producing a clipboard-safe text summary of grid name, generated timestamp, row count, active grouping and active column filters. The fallback path works without the modern Clipboard API, so browser and desktop-client users can hand off support/audit context without exporting a file.
- **2026-07-26 RBAC table handoff increment - E01/E02/E19/E20/E21:** RBAC, sharing, SoD, user-activity and security-review tables now expose **Copy view** through the shared `DataTable` component. The copied summary includes generated timestamp, visible/total row count, active groups, collapsed group count, sort direction and active column filters, giving administrators and auditors a reproducible view description without screenshots.
- **2026-07-26 RBAC table export increment - E01/E02/E15/E19/E20:** shared RBAC/security `DataTable` screens now provide current-view Excel, Word and PDF exports. The generated files use the same governed metadata header as CRM grids, including table name, timestamp, row count, grouping, sort/collapsed-group context and active column filters.
- **2026-07-26 RBAC table workspace increment - E01/E02/E15/E19/E20:** shared RBAC/security `DataTable` screens now render inside their own governed table workspace frame with plain-language help, visible export scope, Audit access and Full size/Restore controls. The behavior lands once in the shared component and applies across role, permission, sharing, SoD, user-activity and security-review tables without page-specific forks.
- **2026-07-26 current-view export audit increment - E01/E02/E15/E19/E20:** browser-generated current-view exports now record server-side export evidence before saving files. CRM grids and shared RBAC/security tables send object/table type, format, exact visible row count, active filters and active groups to the governance export audit trail, while server-generated exports continue to audit inside their producer services.
- **2026-07-26 workflow gate console increment - E06/E14/E15/E19/E20:** the Automation workspace now includes a Workflow Gate Console showing the latest evaluated process-gate records with object type, record id, status, missing prerequisite count, next-step guidance and a review drawer. Pipeline remains the live record-evaluation surface; the console gives administrators and auditors a single place to inspect persisted gate outcomes.
- **2026-07-26 workflow gate grid governance increment - E06/E14/E15/E19/E20:** the Workflow Gate Console now runs on the shared governed `DataTable` layer with object/status/process/state column filters, spread-out grouping, current-view Excel/Word/PDF exports, Copy view, Audit evidence and Full size/Restore. The row-level Review drawer remains available from the same grid, so workflow-gate issues can be filtered, exported and inspected without a one-off table fork.
- **2026-07-26 advanced Jasper portfolio increment - E06/E09/E10/E15:** Reports Studio now presents all twenty-one governed reports in a Jasper report grid with per-column filtering, multi-column grouping, sorting, audit, snapshot copy and full-size review. Eight decision reports add quota attainment, forecast accuracy/bias, stage conversion/velocity, renewal/churn/ARR bridge, exact pipeline movement, account whitespace, Customer 360 and discount-approval governance; browser preview and PDF/Excel/Word downloads share the same tenant-scoped query contract.
- **2026-07-26 five-epic lifecycle enforcement increment - E09/E10/E11/E12/E13:** Contracts, Forecast submissions, Campaigns, Cases and Partner accounts now have active metadata-driven state machines enforced by PostgreSQL triggers for every writer. Their governed commands perform an exact target-transition gate check, persist blocked evidence independently, return layman-language next-step guidance, and refresh the gate record after success. Each workspace exposes Check gates and the shared Review drawer before the operator commits the action.
- **2026-07-26 five-epic platform workflow increment - E14/E15/E16/E17/E18:** Automation simulation readiness, Analytics dashboard refresh, Copilot recommendation disposition, Integration contract verification and Migration validation now use active workflow models and target-specific gate evidence. Import validation explicitly enters `VALIDATING` before `READY_TO_IMPORT` or `FAILED`; every workspace exposes Check gates/Review, and database triggers protect state-changing writes outside the UI. Vendor model, webhook and source-system execution remain intentionally pending.
- **2026-07-26 five-epic governance workflow increment - E19/E20/E21/E22/E23:** Sandbox refresh, Audit evidence export, Mobile sync acknowledgement, BFSI onboarding clearance and Commodity offer now use active lifecycle models with exact target checks, durable gate evidence and PostgreSQL trigger enforcement. The shared workspace UI exposes Check gates/Review on all five surfaces; related screening, credit and approved-term controls remain enforced in the command layer without invoking third-party systems.
- **2026-07-26 runtime resilience fix - E07/E17:** API CORS now runs before authentication so both supported Vite origins (`5173` and `4280`) can read success and security responses, while untrusted origins remain blocked. Activities keeps React hooks in a stable order when a query changes between healthy and unreachable states, removing the reported `Rendered fewer hooks than expected` crash.
- **2026-07-26 record-lock closure - E02/E04/E20:** lease-based record locking now has a complete REST boundary, reusable React lifecycle hook and Contact edit integration. A second editor sees the holder and expiry before typing; heartbeats, idempotent release, administrator override and optimistic-save protection close the concurrency path end to end.
- **2026-07-26 RBAC maker-checker closure - E01/E02/E20:** role, profile, permission-set and permission-set-group grants now submit as HTTP 202 approval requests. Authorization includes a reviewed queue, approve/reject actions, effective-permission inspection and delegation management. Approval plus authoritative grant is one transaction; self/delegated-chain approval is blocked, and platform/tenant principals are both attributable.
- **2026-07-26 Electron rendering closure - E19/E21:** the desktop shell now uses content-area sizing, deterministic 100% zoom and rail-aware header compression. A repeatable offscreen Electron audit covers four high-density routes at four supported viewport sizes and fails on overflow, clipped/off-screen controls or upscaled raster assets; the current run passes 16/16 checks.
- **2026-07-26 core five-epic closure increment - E01/E02/E03/E04/E05:** enabled SAML providers now receive deduplicated 30-day certificate-expiry notifications; RBAC adds point-in-time access-review campaigns whose revoke decisions remove the authoritative grant atomically; reference values resolve by business date from immutable versions; Account 360 exposes explainable weighted health and permission-safe hierarchy roll-ups; lead capture supports single and 1,000-row partial-success ingestion through the same duplicate, scoring, routing and SLA engines. External IdP handshakes and delivery vendors remain intentionally outside this first-party closure.
- **2026-07-26 revenue five-epic closure increment - E06/E07/E08/E09/E10:** opportunity moves now preflight the same pinned lifecycle gates used by the command; engagement exposes permission-filtered immutable email-template versions; active quotes create complete line-and-adjustment-preserving revisions; contracts create one idempotent draft renewal with copied subscriptions; and forecasts save immutable, explainable scenarios without mutating submissions. Microsoft/Google capture, telephony, e-signature and ERP vendor execution remain intentionally outside this first-party closure.
- **2026-07-26 Jasper PDF viewer and reporting-master increment - E03/E15:** Reports Studio keeps the searchable left report library and now separates the selected report into **Report Grid** and an authenticated inline **Document Preview** backed by the actual Jasper PDF. Preview is a read-only use case, downloads remain audited exports, and full view preserves the selected tab. Governed reporting collection, output-format and schedule-frequency masters are seeded for every tenant through `V338`; external document delivery remains intentionally pending.
- **2026-07-26 migration operator closure - E18:** `V342`-`V344` add immutable/restorable mapping revisions, per-object delta checkpoints, linked retry attempts, durable recovery actions and a tenant-checked/entity-allow-listed rollback executor under forced tenant RLS. The canonical migration API and dedicated responsive workspace now expose read-only source discovery, explicit mapping acknowledgement, zero-write dry run, atomic import/delta runs, on-demand source-to-target reconciliation, exact-ledger rollback preview/execution, safe queued cancellation and 100-row server-side issue search/filter. Failed runs never advance checkpoints; retries create a new attempt; rollback can reach only ledger-owned records and explicitly classified owned derivatives, while pre-existing or operator-created references block deletion. The live reversible proof removed all 19 migration-owned targets, cleared five checkpoints and preserved all 9 pre-existing accounts. Live Salesforce, Zoho and HubSpot authentication remains visibly `PENDING_VENDOR`.
- **2026-07-26 report query governance increment - E15:** governed report rows now use server-side search, per-column filters and a maximum/default page size of 100. The semantic grid returns page metadata, while Jasper document preview and PDF/Excel/Word exports execute the same full filtered dataset. Shared `DataTable` grids and the contact directory also render 100 rows per page by default; existing server-paged CRM, CPQ and epic workspaces already use the same page size.
- **2026-07-27 P0E15 certification closure - E15:** projection recovery now performs rebuild plus immediate independent projection/KPI reconciliation under one operator verdict. Grid, PDF, Excel and Word outputs carry the same order-sensitive SHA-256 dataset identity and row count. The production certificate is append-only and passes only with at least one million projected rows, fifty measured 30-day queries, p95 at or below three seconds, zero timeouts and zero drift; smaller environments are explicitly reported as insufficient evidence. The control plane is available in Reports Studio and through tenant-scoped APIs without Docker.

## Skeleton work in detail

What exists in `backend/` today, honestly stated — a walking skeleton proving the architecture's spine, not delivered stories:

- **Tenancy (E01):** `V1__baseline.sql` creates tenant-scoped tables with a generated `tenant_isolation` RLS policy on each (`tenant_id = current_setting('app.tenant_id')`); `TenantContext` / `TenantSessionAspect` bind the tenant from the authenticated principal into the database session — the two-layer enforcement of [ADR-001](architecture/adr/ADR-001-tenancy-isolation.md).
- **Lead→opportunity slice (E05/E06):** `lead`, `account`, `contact`, `pipeline_stage`, `opportunity` and `opportunity_contact_role` tables with `V2__seed_demo.sql` demo data, shaped for the conversion flow in [US-E05-07](product/05-epics-and-stories.md#us-e05-07--lead-conversion-p0-8--fr-led-011).
- **Event backbone (E17 plumbing):** `outbox_event` table and a scheduled Kafka relay configuration per [ADR-003](architecture/adr/ADR-003-event-backbone.md) — transactional outbox from day one, not retrofitted.
- **Notification centre (cross-cutting slice):** `notification` table with tenant RLS and recipient scoping; live feed/unread/read APIs; transactional lead-conversion and stage-movement notices; responsive bell UI with reason, action state, deep link and degraded-state recovery. Email/push, preferences, digests and full record authorization rechecks remain open.

The notification increment has unit/contract coverage and live-stack API/browser evidence recorded in [runtime smoke](../qa/runtime-smoke.md). No complete epic has yet passed its full acceptance catalogue, which is why nothing above is ✅. Run steps are in the [repository README](../README.md).

## Conventions

- Status changes here require the story's Definition of Done ([backlog conventions](product/05-epics-and-stories.md#definition-of-done)) — an epic goes 🟡 when its first story is genuinely done, ✅ when all its stories for the current release scope are.
- Release trains and sprint allocation: [agile delivery plan](product/15-agile-delivery-plan.md).
